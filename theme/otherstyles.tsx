// theme/styles.ts
import { StyleSheet } from "react-native";
import { COLORS } from "./colors";

export const styles = StyleSheet.create({
  // ...your existing styles (primaryBtn, disabledBigBtn, input, etc)

  /**
   * Recipient New Screen
   */
  
});
